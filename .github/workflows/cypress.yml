name: Cypress run

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  cypress-run:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - run: npm install -g yarn
    - run: yarn install
    - run: yarn build

    - name: Run Keycloak
      run: (./start.js & sleep 50) && (npx http-server ./build & sleep 90) && yarn start:cypress-tests

    #- name: Cypress run
    #  uses: cypress-io/github-action@v1
    #  with:
    #    project: ./tests
